[deployment]
config = "configs/deployments/diffusion2one/fruit_blocks_teacher_eef.toml"

[reset]
pose = "configs/poses/a1_collection_start.toml"

[batch]
schema_version = 1
id = "diffusion2one-teacher-banana"
retries_per_prompt = 0
task_ids = ["banana_to_red_plate"]
